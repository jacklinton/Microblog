<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="/bootstrap-3.3.7/docs/favicon.ico">

    <title>Account Profile</title>

    <!-- Bootstrap core CSS -->
    <link href="/bootstrap-3.3.7/dist/css/bootstrap.min.css" rel="stylesheet">

    <link href="/bootstrap-3.3.7/docs/examples/blog/blog.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="/bootstrap-3.3.7/docs/examples/signin/signin.css" rel="stylesheet"> 
    <link rel="stylesheet" type="text/css" href="/styles.css">
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
 </head>

  <body>

  	<div class="blog-masthead">
      <div class="container">
        <nav class="blog-nav">
          <a class="blog-nav-item active" href="/">Home</a>
          <% if session[:id] %>
            <h4 class="blog-nav-item profile-title">View <%= @current_user.username%>'s Profile</h4>
            <a class="blog-nav-item" href="#">Make New Post</a>
            <% if @groupy %>
            <a class="blog-nav-item" href="#"><%= @gname %> Group Page</a>
            <% end %>
          <% else %>
            <a class="blog-nav-item" href="/users/new">Register New Account</a>
          <% end %>
        </nav>
      </div>
    </div>

    <div class="container">
      <div class = "profile-main" id= "profile-main">
        <img src="<%=current_user.avatar%>" class="p-avatar" id="p-avatar">
        <h1 class="profile-title"><%= @current_user.username%></h1>
        <h2 class="profile-title">Group: <%= @current_user.group-name%></h2>
      </div>
      <form class="form-signin" action="/users/edit" type="POST">
        <h2 class="form-signin-heading">Edit Account Details</h2>
        <label for="username" class="sr-only">Pick A Username</label>
        <input type="text" id="username" class="form-control" placeholder="<%= @current_user.username%>" required autofocus>
      	<label for="password" class="sr-only">Password (Case Sensitive)</label>
        <input type="text" id="password" class="form-control" placeholder="password" required autofocus>
        <label for="group_name" class="sr-only">Group(Leave Blank)</label>
        <input type="text" id="group_name" class="form-control" placeholder="group name (optional)">

        <button class="btn btn-lg btn-primary btn-block" type="submit">Register</button>
      </form>

    </div> <!-- /container -->
    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script>window.jQuery || document.write('<script src="/bootstrap-3.3.7/docs/assets/js/vendor/jquery.min.js"><\/script>')</script>
    <script src="/bootstrap-3.3.7/docs/dist/js/bootstrap.min.js"></script>
  </body>
</html>